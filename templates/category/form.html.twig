{% extends 'base.html.twig' %}

{% block title %}{{ form.vars.value.id ? 'Edit Category' : 'Add Category' }}{% endblock %}

{% block body %}
    <h1>{{ form.vars.value.id ? 'Edit Category' : 'Add Category' }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('category_list') }}">← Back to Category List</a>
{% endblock %}
